
%div= flash[:notice] ? flash[:notice] : ''
%div= "Instrument: #{@profile.instrument}"
%div= "Age: #{@profile.age}"
%div= "Gender:#{@profile.gender}"
%div= "Bio: #{@profile.bio}"

= link_to 'Edit', edit_profile_path(current_user), class: 'btn btn-warning'
